<?php ob_start(); ?>

<div class="admin-header">
    <h1 class="admin-title">Manage Episodes</h1>
    <a href="/admin/episodes/add" class="btn btn-primary">+ Add New Episode</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Episode #</th>
                <th>Title</th>
                <th>Description</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($episodes as $episode): ?>
            <tr>
                <td><?= $episode->episode_number ?></td>
                <td><?= htmlspecialchars($episode->title) ?></td>
                <td><?= htmlspecialchars(substr($episode->description, 0, 50)) ?>...</td>
                <td>
                    <span class="status-badge <?= $episode->enabled ? 'status-enabled' : 'status-disabled' ?>">
                        <?= $episode->enabled ? 'Enabled' : 'Disabled' ?>
                    </span>
                </td>
                <td>
                    <a href="/admin/episodes/toggle?id=<?= $episode->id ?>" class="btn btn-small btn-warning">
                        <?= $episode->enabled ? 'Disable' : 'Enable' ?>
                    </a>
                    <a href="/admin/episodes/delete?id=<?= $episode->id ?>" class="btn btn-small btn-danger" 
                       onclick="return confirm('Are you sure you want to delete this episode?')">Delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<?php 
$content = ob_get_clean();
require __DIR__ . '/../../layouts/admin.phtml';
?>
