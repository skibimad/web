<?php ob_start(); ?>

<h1 class="admin-title">Add New Blog Post</h1>

<form method="POST" class="admin-form">
    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" id="title" name="title" required>
    </div>
    
    <div class="form-group">
        <label for="excerpt">Excerpt</label>
        <textarea id="excerpt" name="excerpt" rows="3" required></textarea>
    </div>
    
    <div class="form-group">
        <label for="content">Content</label>
        <textarea id="content" name="content" rows="10" required></textarea>
    </div>
    
    <div class="form-group">
        <label for="image">Featured Image</label>
        <div class="file-upload-container">
            <input type="hidden" id="image" name="image" value="">
            
        </div>
    </div>
    <script>
    document.addEventListener('DOMContentLoaded', function() {
        // Image uploader
        new FileUploader('image', {
            category: 'blog',
            accept: 'image/*',
            uploadUrl: '/api/upload',
            onSuccess: (response) => {
                console.log('Image uploaded:', response.path);
            },
            onError: (error) => {
                alert('Upload error: ' + error);
            }
        });
    });
    </script>
    
    <div class="form-group">
        <label>
            <input type="checkbox" name="published" value="1">
            Publish Now
        </label>
    </div>
    
    <div class="form-actions">
        <button type="submit" class="btn btn-primary">Add Post</button>
        <a href="/admin/blog/blog" class="btn btn-secondary">Cancel</a>
    </div>
</form>

<?php 
$content = ob_get_clean();
require __DIR__ . '/../../layouts/admin.phtml';
?>
