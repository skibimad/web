<?php ob_start(); ?>

<div class="admin-header">
    <h1 class="admin-title">Manage Blog Posts</h1>
    <a href="/admin/blog/add" class="btn btn-primary">+ Add New Post</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Title</th>
                <th>Published</th>
                <th>Date</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($posts as $post): ?>
            <tr>
                <td><?= htmlspecialchars($post->title) ?></td>
                <td>
                    <span class="status-badge <?= $post->published ? 'status-enabled' : 'status-disabled' ?>">
                        <?= $post->published ? 'Yes' : 'No' ?>
                    </span>
                </td>
                <td><?= $post->published_at ? date('M d, Y', strtotime($post->published_at)) : 'N/A' ?></td>
                <td>
                    <span class="status-badge <?= $post->archived ? 'status-disabled' : 'status-enabled' ?>">
                        <?= $post->archived ? 'Archived' : 'Active' ?>
                    </span>
                </td>
                <td>
                    <a href="/admin/blog/edit?id=<?= $post->id ?>" class="btn btn-small">Edit</a>
                    <a href="/admin/blog/archive?id=<?= $post->id ?>" class="btn btn-small btn-warning">
                        <?= $post->archived ? 'Unarchive' : 'Archive' ?>
                    </a>
                    <a href="/admin/blog/delete?id=<?= $post->id ?>" class="btn btn-small btn-danger" 
                       onclick="return confirm('Are you sure you want to delete this post?')">Delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<?php 
$content = ob_get_clean();
require __DIR__ . '/../../layouts/admin.phtml';
?>
