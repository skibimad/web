<?php ob_start(); ?>

<div class="admin-header">
    <h1 class="admin-title">Social Media Links</h1>
    <a href="/admin/social/add" class="btn btn-primary">+ Add New Link</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Order</th>
                <th>Platform</th>
                <th>URL</th>
                <th>Icon</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($links as $link): ?>
            <tr>
                <td><?= $link->display_order ?></td>
                <td><?= htmlspecialchars($link->platform) ?></td>
                <td><?= htmlspecialchars(substr($link->url, 0, 40)) ?>...</td>
                <td><i class="<?= htmlspecialchars($link->icon_class) ?>"></i></td>
                <td>
                    <span class="status-badge <?= $link->enabled ? 'status-enabled' : 'status-disabled' ?>">
                        <?= $link->enabled ? 'Enabled' : 'Disabled' ?>
                    </span>
                </td>
                <td>
                    <a href="/admin/social/edit?id=<?= $link->id ?>" class="btn btn-small">Edit</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<?php 
$content = ob_get_clean();
require __DIR__ . '/../../layouts/admin.phtml';
?>
