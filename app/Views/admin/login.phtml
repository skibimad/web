<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - Skibidi Madness</title>
    <link rel="stylesheet" href="<?= asset('styles/main.css') ?>">
    <link rel="stylesheet" href="<?= asset('styles/admin.css') ?>">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700;900&family=Rajdhani:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        .login-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #0a0a0f 0%, #1e1e28 100%);
        }
        .login-box {
            background: rgba(20, 20, 25, 0.9);
            border: 2px solid var(--color-primary, #ff3366);
            border-radius: 10px;
            padding: 40px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 0 30px rgba(255, 51, 102, 0.3);
        }
        .login-title {
            font-family: 'Orbitron', sans-serif;
            color: var(--color-primary, #ff3366);
            text-align: center;
            margin-bottom: 30px;
            font-size: 2rem;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            color: var(--color-secondary, #00ffcc);
            margin-bottom: 8px;
            font-family: 'Rajdhani', sans-serif;
        }
        .form-group input {
            width: 100%;
            padding: 12px;
            background: rgba(10, 10, 15, 0.8);
            border: 1px solid var(--color-secondary, #00ffcc);
            border-radius: 5px;
            color: #fff;
            font-family: 'Rajdhani', sans-serif;
            font-size: 16px;
        }
        .form-group input:focus {
            outline: none;
            border-color: var(--color-primary, #ff3366);
            box-shadow: 0 0 10px rgba(255, 51, 102, 0.3);
        }
        .btn-login {
            width: 100%;
            padding: 12px;
            background: var(--color-primary, #ff3366);
            border: none;
            border-radius: 5px;
            color: #fff;
            font-family: 'Orbitron', sans-serif;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .btn-login:hover {
            background: var(--color-secondary, #00ffcc);
            color: #0a0a0f;
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(0, 255, 204, 0.4);
        }
        .error-message {
            background: rgba(255, 51, 102, 0.2);
            border: 1px solid var(--color-primary, #ff3366);
            border-radius: 5px;
            padding: 12px;
            color: var(--color-primary, #ff3366);
            margin-bottom: 20px;
            text-align: center;
        }
        .home-link {
            text-align: center;
            margin-top: 20px;
        }
        .home-link a {
            color: var(--color-secondary, #00ffcc);
            text-decoration: none;
            font-family: 'Rajdhani', sans-serif;
        }
        .home-link a:hover {
            color: var(--color-primary, #ff3366);
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="login-box">
            <h1 class="login-title">ADMIN LOGIN</h1>
            
            <?php if (isset($error)): ?>
            <div class="error-message"><?= escape($error) ?></div>
            <?php endif; ?>
            
            <form method="POST" action="/admin/login">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" required autofocus>
                </div>
                
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                
                <button type="submit" class="btn-login">LOGIN</button>
            </form>
            
            <div class="home-link">
                <a href="/">← Back to Home</a>
            </div>
        </div>
    </div>
</body>
</html>
