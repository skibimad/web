<?php ob_start(); ?>

<div class="admin-header">
    <h1 class="admin-title">Manage Heroes</h1>
    <a href="/admin/heroes/add" class="btn btn-primary">+ Add New Hero</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Order</th>
                <th>Name</th>
                <th>Slug</th>
                <th>Abilities</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($heroes as $hero): ?>
            <tr>
                <td><?= $hero->display_order ?></td>
                <td><?= htmlspecialchars($hero->name) ?></td>
                <td><?= htmlspecialchars($hero->slug) ?></td>
                <td><?= htmlspecialchars($hero->abilities) ?></td>
                <td>
                    <span class="status-badge <?= $hero->enabled ? 'status-enabled' : 'status-disabled' ?>">
                        <?= $hero->enabled ? 'Enabled' : 'Disabled' ?>
                    </span>
                </td>
                <td>
                    <a href="/admin/heroes/edit?id=<?= $hero->id ?>" class="btn btn-small">Edit</a>
                    <a href="/admin/heroes/toggle?id=<?= $hero->id ?>" class="btn btn-small btn-warning">
                        <?= $hero->enabled ? 'Disable' : 'Enable' ?>
                    </a>
                    <a href="/admin/heroes/delete?id=<?= $hero->id ?>" class="btn btn-small btn-danger" 
                       onclick="return confirm('Are you sure you want to delete this hero?')">Delete</a>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<?php 
$content = ob_get_clean();
require __DIR__ . '/../../layouts/admin.phtml';
?>
