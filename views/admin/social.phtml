
<div class="admin-header">
    <h1 class="admin-title">Manage Social Links</h1>
    <a href="/admin/social/add" class="btn btn-add">Create</a>
</div>

<!-- Pagination Controls -->
<?php include __DIR__ . '/components/pagination.phtml'; ?>

<!-- Responsive Grid View -->
<div class="admin-cards-grid">
    <?php foreach ($this('socialLinks') as $link): ?>
    <div class="admin-card">
        <div class="admin-card-header">
            <h3 class="admin-card-title"><?= htmlspecialchars($link['platform']) ?></h3>
            <span class="admin-card-date">Order: <?= htmlspecialchars($link['display_order'] ?? '0') ?></span>
        </div>
        
        <div class="admin-card-excerpt">
            <?php if ($link['icon_class']): ?>
                <i class="<?= htmlspecialchars($link['icon_class']) ?>" style="font-size: 2rem; margin-right: 10px;"></i>
            <?php endif; ?>
            <p><a href="<?= htmlspecialchars($link['url']) ?>" target="_blank"><?= htmlspecialchars($link['url']) ?></a></p>
            <p>Status: <?= $link['enabled'] ? '<span style="color: green;">Enabled</span>' : '<span style="color: red;">Disabled</span>' ?></p>
        </div>
        
        <div class="card-actions">
            <a x-tooltip="Edit" href="/admin/social/update?id=<?= $link->getId() ?>" class="btn-small btn-edit x-tooltip"><i class="fas fa-edit"></i></a>
            <a x-tooltip="Delete" href="/admin/social/delete?id=<?= $link->getId() ?>" class="btn-small btn-danger x-tooltip"
               onclick="return confirm('Are you sure you want to delete this social link?')">
                <i class="fas fa-trash-alt"></i>
            </a>
        </div>
    </div>
    <?php endforeach; ?>
</div>

<?php if ($this('socialLinks')->isEmpty()): ?>
<div class="empty-state">
    <p>No social links found. <a href="/admin/social/add">Add your first social link</a>.</p>
</div>
<?php endif; ?>

<!-- Pagination Controls (bottom) -->
<?php include __DIR__ . '/components/pagination.phtml'; ?>
