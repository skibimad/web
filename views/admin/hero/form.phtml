<?php
$hero = $this('hero');
?>
<div class="admin-header">
    <h1 class="admin-title">Hero</h1>
    <a class="btn btn-primary" onclick="history.back()">Back</a>
</div>

<div class="form-container">
    <div class="form-card">

        <form method="post" action="/admin/hero/<?= $hero['id'] ? 'update' : 'put' ?>" enctype="multipart/form-data" class="hero-form">
            <input type="hidden" name="hero[id]" value="<?= $hero['id'] ?>">
            
            <div class="form-group">
                <label for="hero-name">Hero Name</label>
                <input value="<?= htmlspecialchars($hero['name'] ?? '') ?>" type="text" id="hero-name" name="hero[name]" placeholder="Enter hero name" required>
            </div>
            
            <div class="form-group">
                <label for="hero-slug">Slug</label>
                <input value="<?= htmlspecialchars($hero['slug'] ?? '') ?>" type="text" id="hero-slug" name="hero[slug]" placeholder="Enter hero slug (e.g., skibidi-titan)">
            </div>

            <div class="form-group">
                <label for="hero-image">Hero Image</label>
                <?php if ($hero['image']):?>
                    <img height="100px" src="/<?= htmlspecialchars(trim($hero['image'], '/')) ?>" alt="<?= htmlspecialchars($hero['name'] ?? '') ?>" class="card-image">
                <?php endif;?>
                <div class="file-upload-wrapper">
                    <input type="file" id="hero-image" name="hero_image" accept="image/*" class="file-input">
                    <div class="file-upload-area">
                        <div class="upload-icon">📁</div>
                        <p class="upload-text">Click to upload or drag and drop</p>
                        <p class="upload-subtext">PNG, JPG, GIF up to 5MB</p>
                    </div>
                    <div class="file-preview hidden">
                        <img class="preview-image" src="" alt="Preview">
                        <button type="button" class="remove-file">×</button>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label for="hero-video">Hero Video</label>
                <?php if ($hero['video']):?>
                    <video height="100px" controls>
                        <source src="/<?= htmlspecialchars(trim($hero['video'], '/')) ?>" type="video/mp4">
                    </video>
                <?php endif;?>
                <div class="file-upload-wrapper">
                    <input type="file" id="hero-video" name="hero_video" accept="video/*" class="file-input">
                    <div class="file-upload-area">
                        <div class="upload-icon">🎬</div>
                        <p class="upload-text">Click to upload or drag and drop</p>
                        <p class="upload-subtext">MP4, WebM up to 50MB</p>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label for="hero-description">Description</label>
                <textarea id="hero-description" name="hero[description]" class="summernote-editor" placeholder="Write hero description here..."><?= htmlspecialchars($hero['description'] ?? '', ENT_QUOTES, 'UTF-8') ?></textarea>
            </div>
            
            <div class="form-group">
                <label for="hero-abilities">Abilities (comma separated)</label>
                <input value="<?= htmlspecialchars($hero['abilities'] ?? '') ?>" type="text" id="hero-abilities" name="hero[abilities]" placeholder="e.g., Super Strength, Flight, Laser Eyes">
            </div>

            <button type="submit" class="submit-btn">
                Save
            </button>
        </form>
    </div>
</div>
