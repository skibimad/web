
<div class="admin-header">
    <h1 class="admin-title">Manage Heroes</h1>
    <a href="/admin/hero/add" class="btn btn-add">Create</a>
</div>

<!-- Pagination Controls -->
<?php include __DIR__ . '/components/pagination.phtml'; ?>

<!-- Responsive Grid View -->
<div class="admin-cards-grid">
    <?php foreach ($this('heroes') as $hero): ?>
    <div class="admin-card">
        <div class="admin-card-header">
            <h3 class="admin-card-title"><?= htmlspecialchars($hero['name']) ?></h3>
            <span class="admin-card-date"><?= htmlspecialchars($hero['slug'] ?? '') ?></span>
        </div>
        
        <div class="admin-card-excerpt">
            <img height="100rem" src="/<?= htmlspecialchars(trim($hero['image'] ?? '', '/')) ?>" alt="<?= htmlspecialchars($hero['name']) ?>" class="card-image">
            <p><?= htmlspecialchars(substr(strip_tags($hero['description'] ?? ''), 0, 150) . '...') ?></p>
        </div>
        
        <div class="card-actions">
            <a x-tooltip="Edit" href="/admin/hero/update?id=<?= $hero->getId() ?>" class="btn-small btn-edit x-tooltip"><i class="fas fa-edit"></i></a>
            <a x-tooltip="Delete" href="/admin/hero/delete?id=<?= $hero->getId() ?>" class="btn-small btn-danger x-tooltip"
               onclick="return confirm('Are you sure you want to delete this hero?')">
                <i class="fas fa-trash-alt"></i>
            </a>
        </div>
    </div>
    <?php endforeach; ?>
</div>

<!-- Pagination Controls (bottom) -->
<?php include __DIR__ . '/components/pagination.phtml'; ?>
